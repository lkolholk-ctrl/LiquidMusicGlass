<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Pre-Android 13: local audio scan needs READ_EXTERNAL_STORAGE (Stage 7b). -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Haptic Music: тактильные удары в такт музыке (HapticMusicEngine) -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Запрос исключения из оптимизации батареи: без него Doze душит фоновые
         декод-потоки и сеть стриминга (лаги/цикличка при погашенном экране) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
          android:name=".App"
          android:allowBackup="true"
          android:icon="@drawable/ic_launcher"
          android:roundIcon="@drawable/ic_launcher"
          android:label="LiquidMusicGlass"
          android:supportsRtl="true"
          android:usesCleartextTraffic="true"
          android:enableOnBackInvokedCallback="true"
          android:theme="@style/Theme.LiquidMusicGlass">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".ui.crash.CrashActivity"
            android:exported="false"
            android:theme="@style/Theme.LiquidMusicGlass" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="liquidmusicglass"
                      android:host="oauth"
                      android:path="/icm" />
            </intent-filter>
        </activity>

        <service
            android:name=".engine.AudioService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            android:priority="1000">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <!-- MediaBrowserService action removed: on OEM devices (Honor/Huawei)
                     the system media-resumption binds a browsable service at cold
                     start, spinning up AudioService.onCreate -> ExoPlayer (+codec) and
                     contending for CMA, contributing to the startup ANR. App playback
                     connects via the MediaSessionService action above and is
                     unaffected; only system "resume last media" browsing is dropped. -->
            </intent-filter>
        </service>

        <!-- Playlist Import Foreground Service -->
        <service
            android:name=".data.playlistimport.PlaylistImportService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Playlist Download Foreground Service -->
        <service
            android:name=".engine.PlaylistDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- MediaButtonReceiver required for OEM devices (Huawei/Honor) to resolve ComponentName -->
        <receiver android:name="androidx.media3.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>