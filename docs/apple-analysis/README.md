# Разбор Apple Music → план для LMG

Состояние на 27.07.2026.

## Что здесь лежит

Исходные отчёты по разбору Apple Music for Android (сделаны агентами 27.07):

- `01-lyrics-data-format.md` — форматы лирики, роли вокала, тайминги, кэш
- `02-audio-engine.md` — аудио-движок, обработка, gapless, кэш
- `03-lyrics-screen.md` — экран лирики: вёрстка, анимации, прокрутка
- `04-queue-screen.md` — экран очереди, сравнение с нашим `QueueSheet.kt`
- `AppleMusic_Lyrics_RE.md` — более ранний разбор лирики

Планы адаптации под наш код (пишутся агентами, могут быть неполными):

- `plan-01-lyrics-data.md`
- `plan-02-audio-engine.md`
- `plan-03-lyrics-screen.md`
- `plan-04-queue-screen.md`

## Как это делалось

Четыре агента, по одному на отчёт. Каждому: прочитать отчёт, прочитать наш код в
своей зоне, выдать ранжированный список реализуемого — не копируя код 1:1.
Правки в код они не вносили намеренно: зоны пересекаются (01 и 03 обе про
лирику, 02 и 04 обе задевают плеер), четыре параллельных редактора сделали бы
кашу. Реализация — потом, батчами по 2-3 пункта.

## Ограничения, заданные владельцем

- Пословных таймингов лирики пока нет: ICM отдаёт их только по премиум-подписке,
  менеджеру написали, ответа ждём. Лирику делаем построчной, с заделом под
  пословные.
- Шрифты SF Pro и иконки SF Symbols использовать нельзя.
- Светлая и тёмная темы обе обязательны.
- Числовые значения — в `ui/theme/LiquidMetrics.kt`, не хардкодом.
- Тумблеры и ползунки в настройках не менять.
- Sing/караоке убран — работал не на всех песнях.
- Пространственное аудио отложено.
- Android Auto отложен: browsable-сервис удалён намеренно, вызывал ANR на
  Honor/Huawei.
- Кроссфейд в форке media3 владельца устраивает — больше не шлифуем.
- UI перенимаем по принципам поведения, не пиксельно: «потом внесём наши правки,
  чтоб не было схожести».

## Сделано

- **Батч 1** (коммит «Починки по разбору Apple Music»): негативный кэш лирики
  раздельно для «текста нет» и «не дозвонились»; единая фабрика HTTP (настройка
  12/15с раньше не применялась); выброс битой записи кэша; пустое состояние
  очереди; подписка на избранное в remember.
- **Батч 2** (коммит «Очередь: перемешивание, повтор, константы, жесты»):
  перемешивание применяется к самой очереди, а не к флагу плеера; гейт
  дозаправки волной при повторе; константы очереди в LiquidMetrics/LiquidSurfaces
  и плотность затемнения по теме; смещение перетаскивания читается только в
  graphicsLayer; наводка списка; режим «редактировать нельзя» на JUCE; подъём
  строки; подложка свайпа; контекстный текст пустого состояния; TalkBack.
  Проверено владельцем на устройстве — перемешивание работает.

## Что дальше

1. **Батч 3 — экран лирики** (в работе): промах прокрутки на строку, пропажа
   нижней растушёвки вместе с контролами, отсутствие светлой темы, опережающая
   прокрутка, плоские альфы, зона касания, TalkBack, мёртвый код.
2. **ОТЛОЖЕНО, НЕ ЗАБЫТЬ — секции очереди.** Самый заметный оставшийся кусок по
   очереди: «добавлено вручную / остаток альбома / волна» со своими заголовками.
   Требует модели происхождения треков в движке (`plan-04`, пункты 1, 2, 7, 10,
   13) — два числа-границы вместо параллельного списка, девять точек мутации
   очереди. Владелец попросил вернуться к этому сразу после батча 3.
3. Остальные пункты четырёх планов — см. `plan-0*.md`.
4. Остальные экраны под новый стиль: главная, библиотека, история, поиск.
5. Общий плейлист: UI добавления трека из приложения (серверное API уже есть).
