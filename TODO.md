# TODO

## ⛔ ВЕРНУТЬ ПЕРЕД РЕЛИЗОМ — временно отключено ради скорости отладочных сборок

На время отладки вся защита и оптимизация выключены (НЕ оставлять в финале!):

- [x] `PROTECTION_ENABLED = true` в `MainActivity.kt` — включено.
- [x] R8/минификация: `isMinifyEnabled = true` и `isShrinkResources = true` в
  `app/build.gradle.kts` (release) — включено; keep-правила расписаны в
  `app/proguard-rules.pro` (JNI-мосты, Room, kotlinx.serialization, LiteRT).

---

Отложенные задачи по безопасности (подробности — в `REVIEW.md`).

## 🔴 Критично

- [ ] **Ротировать утёкшие секреты.**
  - API-ключ ICM лежит в открытом виде в комментарии `app/src/main/cpp/icmkey.c`
    (`pk_msng_...`); XOR-«шифрование» рядом тривиально обратимо. Считать ключ
    скомпрометированным и перевыпустить на стороне ICM.
  - Пароль release-keystore (`St@skrasikov1`) остаётся в истории git —
    сменить пароль keystore.
- [ ] Убрать plaintext-ключ из комментария в `icmkey.c` после ротации.
- [ ] Добавить GitHub Secrets для подписи (`KEYSTORE_PASSWORD`, `KEY_PASSWORD`,
  `KEY_ALIAS`, `KEYSTORE_PATH`) — иначе release-сборка в CI не подпишется.

## 🟡 Стоит знать

- [ ] **Анти-тампер слой отключён.** `nativeVerifySignature()` всегда
  возвращает `JNI_TRUE`, `check_ptrace()` — `0` (сделано ради совместимости
  с OEM-песочницами Honor/Xiaomi). Решить: вернуть реальную проверку подписи
  с белым списком проблемных OEM — или явно задокументировать, что отключено
  намеренно.
- [ ] Гейтить отладочное логирование (эхо тел ответов в logcat в
  `IcmApiFileLogger`) по `BuildConfig.DEBUG`.
- [ ] Проверить, что `Appupdater.kt` берёт APK обновления по HTTPS и проверяет
  подпись скачанного APK перед установкой.

## 🎨 UI / производительность

- [ ] **Аура: дым должен ПЛАВНО клубиться под бас, а не дрыгаться.** Сейчас под
  бас движение местами дёрганое/нервное. Нужно мягкое клубление (billowing) —
  плавная реакция без рывков. Вероятные точки: огибающая баса
  (`BASS_ATTACK/RELEASE`, кап шага) и связка `uBass`→скорость/вдох в
  `AuraBackground.kt` — возможно, сам сигнал `AudioReactor.low` слишком
  «зубчатый» и нужна доп. фильтрация/более медленные коэффициенты.

- [ ] **Лирика: не торопиться + «waiting» в проигрышах.** Во вступлениях,
  длинных паузах и инструментальных проигрышах (когда вокала нет) заливка не
  должна спешить/уползать вперёд, а вместо текста показывать индикатор
  ожидания — пульсирующие точки (● ● ●). Сейчас длительность строки уже
  капается по числу слов (`LyricsTimeProcessor.currentLineProgress`), но
  отдельного «waiting»-состояния между строками нет. Реализация: детектить
  gap до следующей строки (`line.timeMs` следующей − конец текущей) и при
  крупном разрыве рендерить waiting-индикатор в `LyricsScreen` вместо/над
  активной строкой.
- [ ] **Останавливать анимацию ауры, когда главный экран не виден** (как у
  Яндекса): уходишь с «Моей волны» (например, в Плейлисты/Профиль/полный
  плеер) → шейдер/анимация на паузе; возвращаешься → продолжается. Сейчас
  фуллскрин AGSL-шейдер крутится 60fps постоянно, пока экран в композиции —
  это лишняя нагрузка/нагрев. Реализация: завязать цикл кадров
  (`withInfiniteAnimationFrameMillis`/`produceState` в `AuraBackground`) на
  видимость (Lifecycle `ON_PAUSE`/`ON_RESUME` или `selectedIndex == 0` +
  отсутствие открытых оверлеев), а также не анимировать, когда музыка на паузе.

## 🎬 Видеоклипы Apple Music (клиент ГОТОВ — ждём фикс ICM)

Реализовано и работает с нашей стороны: поиск клипов встроен в обычный
поиск (сегмент «Video» рядом с Apple/VK/All, `ClipResultsSection` в
`SearchScreen.kt`), `IcmApi.searchClips/resolveClip/pollClipJob/
resolveClipStreamUrl` (резолв с поллингом холодного до 90с), воспроизведение
клипа основным ExoPlayer (`PlayerController.playClip`), видео на `SurfaceView`
вместо обложки в `FullPlayer` (`attachVideoSurface`, флаг `isVideoClip`).
Scope `clips` включён партнёру, premium активен.

**БЛОКЕР на стороне ICM (уточнён 23.07.2026, отчёт отправлен менеджеру):**
менеджер ответил «у нас Local Bot API, лимит 2 ГБ» и прислал клип 48 МБ через
@byicmbot — но это про БОТА. Партнёрский `GET /clips/stream/{file_id}` ходит в
ОБЛАЧНЫЙ Bot API getFile (лимит 20 МБ): проверено строго по доке (resolve
quality=720/1080/360 → ready, стрим без заголовков) — на всех качествах
`404 "Telegram file error: file is too big"`, причём 360p падает так же ⇒
quality не пережимает файл, отдаётся один file_id большого исходника.
Короткие тизеры (<20 МБ) играют нормально. Репро: линкованный юзер
`lg_c618bd5b4d8b4436`, клип 1514195718 (Boulevard Depo — Angry toy$),
x-request-id `a3ee44776ecf456a`. Просьба к ICM: перевести `/clips/stream`
на тот же Local Bot API, что у бота. Как переведут — у нас заработает без
правок.

- [ ] Когда ICM починят: перепроверить полноразмерные клипы, при желании
  понизить дефолт качества (сейчас 1080 в `resolveClipStreamUrl`/`playClip`).

## 🖥️ Альбомная/планшетная адаптация

Готово и работает: плеер + лирика + Queue (сплит, обложка слева / контент
справа), поворот без пересоздания (`configChanges`), фундамент
`rememberWindowInfo()` (брейкпоинты по ширине), альбомная главная карточным
видом (`LandscapeHome.kt` + `SideBar.kt`), первый проход по остальным экранам
(4 субагента: ограничение ширины / больше колонок). Всё под `useSideBySide`,
compact-портрет не тронут.

Раскладка — по референсу друга (сайдбар слева, контент по центру, панель
Playing Now + Queue справа, полноширинный нижний мини-плеер), СТИЛЬ — наш
(`LiquidTheme.colors`, `LiquidGlyphs`, glass-компоненты).

Сделано (итерация «референс друга + наш UI», 2026-07-23):
- [x] **Свет + тьма в альбоме.** `SideBar` и `LandscapeHome` больше не
  хардкодят тёмные цвета — палитра из `LiquidTheme.colors` (панели =
  `cardSurface`, фон = `settingsBackground`, текст/акцент/дивайдеры темовые).
  Убран форс `0xFF0A0A0A` в `AppRoot` Row.
- [x] **Полноширинный нижний мини-плеер** `player/LandscapeBottomBar.kt`:
  обложка+инфо слева, prev/play/next + прогресс с временем по центру,
  shuffle/repeat/favorite/queue справа. Реальные действия `PlayerController`.
  Показывается на всех альбомных вкладках (в т.ч. главной), прячется под
  полным плеером. Заменил узкий портретный мини-плеер в широком окне.
- [x] Шапка главной: кнопка поиска (→ `onNavigateToSearch`), секции с
  иконками (History/Heart/Queue), длительности треков в списках.

Осталось:
- [ ] **Обрезанные пункты, которые не листаются** — проверить каждый экран
  (настройки, профиль, эквалайзер, Яндекс, детальные): некоторые агентские
  `widthIn`-колонки могли остаться без `verticalScroll`/`LazyColumn` по высоте.
- [ ] Пройтись по КАЖДОМУ экрану в альбоме на светлой и тёмной теме, на
  телефоне-альбоме и планшете (expanded).
- Файлы: `ui/WindowInfo.kt`, `ui/navigation/SideBar.kt`,
  `ui/screens/LandscapeHome.kt`, `ui/player/LandscapeBottomBar.kt`,
  `ui/AppRoot.kt` (Row-каркас + нижний бар), `player/FullPlayer.kt`
  (сплит-эталон), + экраны с ветками `if (win.useSideBySide)`.
