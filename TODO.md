# TODO

Пользователь подтвердил (24.07.2026): старые задачи из этого файла сделаны —
файл вычищен. История — в git.

## ⛔ Перед релизом (секреты — не UI)

- [ ] **Ротировать утёкшие секреты**: API-ключ ICM в комментарии
  `app/src/main/cpp/icmkey.c` (считать скомпрометированным, перевыпустить у
  ICM) и пароль release-keystore в истории git — сменить.
- [ ] Убрать plaintext-ключ из `icmkey.c` после ротации.
- [ ] GitHub Secrets для подписи (`KEYSTORE_PASSWORD`, `KEY_PASSWORD`,
  `KEY_ALIAS`, `KEYSTORE_PATH`) — иначе release в CI не подпишется.

## 🎬 Видеоклипы — ждём фиксы ICM (клиент готов)

- [x] Стрим клипов: ICM починили (Local Bot API) — работает, проверено.
- [ ] **Блокер №2 у ICM (отправлено менеджеру 23.07.2026): quality не
  транскодирует** — все качества отдают один исходник (~10 Мбит/с; Godzilla
  336 МБ, есть клипы 1+ ГБ) → буферизация на мобильной сети. Просили транскод
  720p/480p, в идеале HLS. Наш дефолт уже 720 — заработает сразу после фикса.
