# TODO

Файл вычищен 24.07.2026 (подтверждено пользователем: ключ ICM давно на
сервере — см. серверный брокер, клиент секретов не держит; прочие старые
пункты неактуальны). История — в git.

## 🎬 Видеоклипы Яндекса — НА ПРОВЕРКЕ (тест только на устройстве)

Реализован нативный путь: `YandexMusicClient.fetchArtist` парсит `brief-info.clips`
→ раздел «Video clips» на странице артиста ЯМ; тап → `resolveClipHls(playerId)`
(видеохостинг VH → .m3u8) → `PlayerController.playClip` (HLS через новый
`media3-exoplayer-hls`) на нашем Surface. Резолв — на устройстве (VH гео-залочен,
с VPS 451). Первый клип на первое устройство пишет СЫРОЙ ответ VH и clips[0] в
DebugLog.

- [ ] Проверить на телефоне: играет ли VH-HLS. Если тост «Clip is DRM-protected»
  или «Couldn't load» — снять DebugLog (что пришло из VH) и доработать резолвер
  (возможно другой `service=`/эндпоинт), либо признать Widevine → пивот на
  ICM-матчинг (искать клип по артист+название в ICM `/clips`).

## 🎬 Видеоклипы Apple (ICM) — ждём фикс ICM (клиент готов)

- [ ] **Блокер у ICM (отправлено менеджеру 23.07.2026): quality не
  транскодирует** — все качества отдают один исходник (~10 Мбит/с; Godzilla
  336 МБ, есть клипы 1+ ГБ) → буферизация на мобильной сети. Просили транскод
  720p/480p, в идеале HLS. Наш дефолт уже 720 — заработает сразу после фикса.
